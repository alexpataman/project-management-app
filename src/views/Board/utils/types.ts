export type ColumnEditForm = {
  name: string;
};

export type TaskEditForm = {
  name: string;
  description: string;
};

export type TaskParams = {
  title: string;
  description: string;
  responsible: string;
};
